diff --git a/src/fb-cpp/Attachment.h b/src/fb-cpp/Attachment.h
index 957b6b5..26587a3 100644
--- a/src/fb-cpp/Attachment.h
+++ b/src/fb-cpp/Attachment.h
@@ -52,7 +52,7 @@ namespace fbcpp
 	///
 	/// Represents options used when creating an Attachment object.
 	///
-	class AttachmentOptions final
+	class AttachmentOptions
 	{
 	public:
 		///
@@ -200,7 +200,7 @@ namespace fbcpp
 			return *this;
 		}
 
-	private:
+	protected:
 		std::optional<std::string> connectionCharSet;
 		std::optional<std::string> userName;
 		std::optional<std::string> password;
@@ -216,7 +216,7 @@ namespace fbcpp
 	/// The Attachment must exist and remain valid while there are other objects using it, such as Transaction and
 	/// Statement.
 	///
-	class FB_CPP_EXPORT Attachment final
+	class FB_CPP_EXPORT Attachment
 	{
 	public:
 		///
@@ -426,11 +426,11 @@ namespace fbcpp
 		std::optional<T> queryFirstRowAs(
 			Transaction& transaction, std::string_view sql, const StatementOptions& options, const Params& params);
 
-	private:
+	protected:
 		void disconnectOrDrop(bool drop);
 		RowSet queryPreparedRowSet(Statement& statement, Transaction& transaction, unsigned maxRows);
 
-	private:
+	protected:
 		Client* client;
 		FbRef<fb::IAttachment> handle;
 	};
diff --git a/src/fb-cpp/BackupManager.h b/src/fb-cpp/BackupManager.h
index 1f0ba9b..679f20b 100644
--- a/src/fb-cpp/BackupManager.h
+++ b/src/fb-cpp/BackupManager.h
@@ -82,7 +82,7 @@ namespace fbcpp
 		///
 		BackupOptions& addBackupFile(const std::string& value)
 		{
-			backupFiles.emplace_back(value, std::nullopt);
+			backupFiles.push_back({value, std::nullopt});
 			return *this;
 		}
 
@@ -94,7 +94,7 @@ namespace fbcpp
 			if (!backupFiles.empty() && !backupFiles.back().length)
 				throw std::invalid_argument{"Cannot add a backup file with length after a backup file without length"};
 
-			backupFiles.emplace_back(value, length);
+			backupFiles.push_back({value, length});
 			return *this;
 		}
 
@@ -201,7 +201,7 @@ namespace fbcpp
 		///
 		RestoreOptions& addDatabaseFile(const std::string& value)
 		{
-			databaseFiles.emplace_back(value, std::nullopt);
+			databaseFiles.push_back({value, std::nullopt});
 			return *this;
 		}
 
@@ -216,7 +216,7 @@ namespace fbcpp
 					"Cannot add a database file with length after a database file without length"};
 			}
 
-			databaseFiles.emplace_back(value, length);
+			databaseFiles.push_back({value, length});
 			return *this;
 		}
 
diff --git a/src/fb-cpp/Blob.h b/src/fb-cpp/Blob.h
index a3a4407..a521597 100644
--- a/src/fb-cpp/Blob.h
+++ b/src/fb-cpp/Blob.h
@@ -49,7 +49,7 @@ namespace fbcpp
 	///
 	/// Represents a Firebird blob identifier.
 	///
-	class BlobId final
+	class BlobId
 	{
 	public:
 		/// Returns whether this blob identifier is empty.
@@ -100,7 +100,7 @@ namespace fbcpp
 	///
 	/// Additional options used when creating or opening blobs.
 	///
-	class BlobOptions final
+	class BlobOptions
 	{
 	public:
 		///
@@ -231,7 +231,7 @@ namespace fbcpp
 			return *this;
 		}
 
-	private:
+	protected:
 		std::vector<std::uint8_t> bpb;
 		std::optional<BlobType> type;
 		std::optional<BlobType> sourceType;
@@ -265,7 +265,7 @@ namespace fbcpp
 	///
 	/// Provides read and write access to Firebird blobs.
 	///
-	class FB_CPP_EXPORT Blob final
+	class FB_CPP_EXPORT Blob
 	{
 	public:
 		///
@@ -420,10 +420,10 @@ namespace fbcpp
 		///
 		void close();
 
-	private:
+	protected:
 		std::vector<std::uint8_t> prepareBpb(const BlobOptions& options);
 
-	private:
+	protected:
 		Attachment& attachment;
 		Transaction& transaction;
 		BlobId id;
diff --git a/src/fb-cpp/Statement.cpp b/src/fb-cpp/Statement.cpp
index 6db794e..98d671e 100644
--- a/src/fb-cpp/Statement.cpp
+++ b/src/fb-cpp/Statement.cpp
@@ -31,6 +31,15 @@ using namespace fbcpp;
 using namespace fbcpp::impl;
 
 
+Statement::Statement(Attachment& attachment)
+	: attachment{&attachment},
+	  statusWrapper{attachment.getClient()},
+	  calendarConverter{attachment.getClient()},
+	  numericConverter{attachment.getClient()}
+{
+}
+
+
 Statement::Statement(
 	Attachment& attachment, Transaction& transaction, std::string_view sql, const StatementOptions& options)
 	: attachment{&attachment},
@@ -49,7 +58,7 @@ Statement::Statement(
 	if (options.getPrefetchPlan())
 		flags |= fb::IStatement::PREPARE_PREFETCH_DETAILED_PLAN;
 
-	statementHandle.reset(attachment.getHandle()->prepare(&statusWrapper, transaction.getHandle().get(),
+	statementHandle.reset(attachment.getHandle()->prepare(&statusWrapper, nullptr,
 		static_cast<unsigned>(sql.length()), sql.data(), options.getDialect(), flags));
 
 	if (options.getCursorName().has_value())
@@ -96,6 +105,9 @@ Statement::Statement(
 
 		const auto count = metadata->getCount(&statusWrapper);
 		descriptors.reserve(count);
+		auto safeString = [](const char* s) -> std::string {
+			return s ? std::string(s) : std::string();
+		};
 
 		for (unsigned index = 0u; index < count; ++index)
 		{
@@ -107,10 +119,10 @@ Statement::Statement(
 				.offset = 0,
 				.nullOffset = 0,
 				.isNullable = static_cast<bool>(metadata->isNullable(&statusWrapper, index)),
-				.name = metadata->getField(&statusWrapper, index),
-				.relation = metadata->getRelation(&statusWrapper, index),
-				.alias = metadata->getAlias(&statusWrapper, index),
-				.owner = metadata->getOwner(&statusWrapper, index),
+				.name = safeString(metadata->getField(&statusWrapper, index)),
+				.relation = safeString(metadata->getRelation(&statusWrapper, index)),
+				.alias = safeString(metadata->getAlias(&statusWrapper, index)),
+				.owner = safeString(metadata->getOwner(&statusWrapper, index)),
 				.charSetId = metadata->getCharSet(&statusWrapper, index),
 				.subType = metadata->getSubType(&statusWrapper, index),
 			};
@@ -235,6 +247,16 @@ void Statement::free()
 {
 	assert(isValid());
 
+	closeCursor();
+
+	statementHandle->free(&statusWrapper);
+	statementHandle.reset();
+}
+
+void Statement::closeCursor()
+{
+	assert(isValid());
+
 	if (resultSetHandle)
 	{
 		resultSetHandle->close(&statusWrapper);
@@ -242,22 +264,22 @@ void Statement::free()
 	}
 
 	currentRow = false;
-	statementHandle->free(&statusWrapper);
-	statementHandle.reset();
 }
 
 std::string Statement::getLegacyPlan()
 {
 	assert(isValid());
 
-	return statementHandle->getPlan(&statusWrapper, false);
+	const char* plan = statementHandle->getPlan(&statusWrapper, false);
+	return plan ? plan : "";
 }
 
 std::string Statement::getPlan()
 {
 	assert(isValid());
 
-	return statementHandle->getPlan(&statusWrapper, true);
+	const char* plan = statementHandle->getPlan(&statusWrapper, true);
+	return plan ? plan : "";
 }
 
 bool Statement::execute(Transaction& transaction)
diff --git a/src/fb-cpp/Statement.h b/src/fb-cpp/Statement.h
index 1cf1d38..2254ea5 100644
--- a/src/fb-cpp/Statement.h
+++ b/src/fb-cpp/Statement.h
@@ -79,6 +79,10 @@ namespace fbcpp
 	///
 	/// @brief Distinguishes the semantic category of the prepared SQL statement.
 	///
+	#ifdef DELETE
+#undef DELETE
+#endif
+
 	enum class StatementType : unsigned
 	{
 		///
@@ -142,11 +146,13 @@ namespace fbcpp
 	///
 	/// Prepares, executes, and fetches SQL statements against a Firebird attachment.
 	///
-	class FB_CPP_EXPORT Statement final : private impl::ParameterSetter<Statement, false>
+	class FB_CPP_EXPORT Statement : private impl::ParameterSetter<Statement, false>
 	{
 		friend class RowSet;
 		friend class impl::ParameterSetter<Statement, false>;
 
+	public:
+		explicit Statement(Attachment& attachment);
 	public:
 		///
 		/// Prepares an SQL statement.
@@ -318,6 +324,7 @@ namespace fbcpp
 		/// @brief Releases the prepared handle and any associated result set.
 		///
 		void free();
+		void closeCursor();
 
 		///
 		/// @brief Retrieves the textual legacy plan if the server produced one.
@@ -802,7 +809,7 @@ namespace fbcpp
 			return outRow->get<V>(index);
 		}
 
-	private:
+	protected:
 		const std::vector<Descriptor>& getSetterDescriptors() const noexcept
 		{
 			return inDescriptors;
@@ -830,7 +837,7 @@ namespace fbcpp
 			currentRow = false;
 		}
 
-	private:
+	protected:
 		Attachment* attachment;
 		impl::StatusWrapper statusWrapper;
 		impl::CalendarConverter calendarConverter;
diff --git a/src/fb-cpp/Transaction.cpp b/src/fb-cpp/Transaction.cpp
index de8e6f4..233e1ce 100644
--- a/src/fb-cpp/Transaction.cpp
+++ b/src/fb-cpp/Transaction.cpp
@@ -32,7 +32,15 @@ using namespace fbcpp;
 using namespace fbcpp::impl;
 
 
-static FbUniquePtr<fb::IXpbBuilder> buildTpb(
+Transaction::Transaction(Client& client)
+	: client{client},
+	  handle{nullptr},
+	  state{TransactionState::ROLLED_BACK}
+{
+}
+
+
+FbUniquePtr<fb::IXpbBuilder> Transaction::buildTpb(
 	fb::IMaster* master, StatusWrapper& statusWrapper, const TransactionOptions& options)
 {
 	auto tpbBuilder = fbUnique(master->getUtilInterface()->getXpbBuilder(&statusWrapper, fb::IXpbBuilder::TPB,
@@ -239,6 +247,21 @@ void Transaction::rollbackRetaining()
 	handle->rollbackRetaining(&statusWrapper);
 }
 
+void Transaction::start(Attachment& attachment, const TransactionOptions& options)
+{
+	assert(!isValid());
+
+	const auto master = client.getMaster();
+	StatusWrapper statusWrapper{client};
+
+	auto tpbBuilder = buildTpb(master, statusWrapper, options);
+	const auto tpbBuffer = tpbBuilder->getBuffer(&statusWrapper);
+	const auto tpbBufferLen = tpbBuilder->getBufferLength(&statusWrapper);
+
+	handle.reset(attachment.getHandle()->startTransaction(&statusWrapper, tpbBufferLen, tpbBuffer));
+	state = TransactionState::ACTIVE;
+}
+
 void Transaction::prepare()
 {
 	prepare(std::span<const std::uint8_t>{});
diff --git a/src/fb-cpp/Transaction.h b/src/fb-cpp/Transaction.h
index 6e9b2f3..e9914b3 100644
--- a/src/fb-cpp/Transaction.h
+++ b/src/fb-cpp/Transaction.h
@@ -30,6 +30,7 @@
 #include "SmartPtrs.h"
 #include <memory>
 #include <optional>
+#include <cstdint>
 #include <span>
 #include <stdexcept>
 #include <string>
@@ -46,6 +47,11 @@ namespace fbcpp
 {
 	class Attachment;
 
+	namespace impl
+	{
+		class StatusWrapper;
+	}
+
 	///
 	/// Transaction isolation level.
 	///
@@ -118,7 +124,7 @@ namespace fbcpp
 	///
 	/// Represents options used when creating a Transaction object.
 	///
-	class TransactionOptions final
+	class TransactionOptions
 	{
 	public:
 		///
@@ -288,7 +294,7 @@ namespace fbcpp
 			return *this;
 		}
 
-	private:
+	protected:
 		std::vector<std::uint8_t> tpb;
 		std::optional<TransactionIsolationLevel> isolationLevel;
 		std::optional<TransactionReadCommittedMode> readCommittedMode;
@@ -349,8 +355,10 @@ namespace fbcpp
 	/// being committed or rolled back (and not prepared), it will be automatically
 	/// rolled back.
 	///
-	class FB_CPP_EXPORT Transaction final
+	class FB_CPP_EXPORT Transaction
 	{
+	public:
+		explicit Transaction(Client& client);
 	public:
 		///
 		/// Constructs a Transaction object that starts a transaction in the specified
@@ -494,7 +502,11 @@ namespace fbcpp
 		///
 		void rollbackRetaining();
 
-	private:
+		void start(Attachment& attachment, const TransactionOptions& options = {});
+
+		static FbUniquePtr<fb::IXpbBuilder> buildTpb(fb::IMaster* master, impl::StatusWrapper& statusWrapper, const TransactionOptions& options);
+
+	protected:
 		Client& client;
 		FbRef<fb::ITransaction> handle;
 		TransactionState state = TransactionState::ACTIVE;
